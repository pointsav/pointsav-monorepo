# app-infrastructure-onprem

Cartucho de infraestructura para nodos locales (Laptop A).

Ejecuta en `os-infrastructure`. Desplegado como `fleet-infrastructure-onprem` en el nivel
de presentación y `fleet-infrastructure-onprem-1` en la Red Privada de PointSav.

Parte de la familia `app-infrastructure-*`.
