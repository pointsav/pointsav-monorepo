[package]
name = "service-fs"
version = "1.0.0"
edition = "2021"
authors = ["PointSav Digital Systems"]
description = "Unikernel-ready file system gatekeeper. Enforces WORM storage boundaries."

[workspace]

[dependencies]
tokio = { version = "1", features = ["full"] }
warp = "0.3"
bytes = "1.0"
