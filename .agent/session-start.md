---
schema: foundry-session-start-v1
archive: project-orgcharts
updated: 2026-05-14
---

# Session start — project-orgcharts

> Step 8 of the session start ritual (AGENT.md §Session start).
> Engine-agnostic — Claude Code and Gemini CLI both read this.

## This archive at a glance

- **Mission:** Author Woodfine corporate org charts (and corporate visualizations over time) using the PointSav design system. Every UI pattern that emerges during chart authoring — node shapes, hierarchy connectors, role badges, brand placement — is backfilled as design-system tokens/components. Corporate document archive shared scope with `project-bookkeeping`.
- **Active branch:** `cluster/project-orgcharts`
- **Inbox:** read `.agent/inbox.md` (step 4 — already done before this file)
- **In-flight plans:** none (check `.agent/plans/` for any new files)

## Known gotchas

- Shared document scope with `project-bookkeeping` — coordinate if touching GUIDE artifacts that span both.
- UI patterns discovered here should be backfilled to `pointsav-design-system` via `project-design`. They do not go directly into the design system from this archive.
- Commit via `~/Foundry/bin/commit-as-next.sh` only (staging-tier).

## Last session handoff

*No prior handoff recorded.*
