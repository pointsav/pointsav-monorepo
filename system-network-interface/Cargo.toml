[package]
name = "system-network-interface"
version = "0.1.0"
edition = "2021"
authors = ["PointSav Engineering <engineering@pointsav.com>"]
description = "Layer 1b Vertical Adapter for VirtIO Networking"

[lib]
crate-type = ["staticlib"]

[dependencies]
# No standard library allowed for Tier-6 components
