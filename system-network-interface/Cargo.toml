[package]
name = "system-network-interface"
version = "1.1.0"
edition = "2021"
authors = ["PointSav Digital Systems"]
description = "Control Plane bridging HTTP F8 Terminal and F12 Ingestion."

[workspace]

[dependencies]
tokio = { version = "1", features = ["full"] }
warp = "0.3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
bytes = "1.0"
