[package]
name = "ingress-harvester"
version = "1.1.0"
edition = "2021"

[dependencies]
imap = "2.4"
native-tls = "0.2"
dotenv = "0.15"

# PHYSICAL THROTTLE: Disable heavy debug symbols and optimizations
[profile.dev]
opt-level = 0
debug = false
split-debuginfo = "off"
