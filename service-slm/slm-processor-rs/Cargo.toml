[package]
name = "slm-processor-rs"
version = "4.0.0"
edition = "2021"
authors = ["PointSav Digital Systems"]
description = "Leapfrog 2030 Semantic ALU. Enforces strict ChatML and Rust Enum Traps."

[workspace]

[dependencies]
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.11", default-features = false, features = ["json", "rustls-tls"] }
serde_json = "1.0"
