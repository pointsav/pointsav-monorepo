# readme-project

The README inside a project directory (e.g., a crate inside a
monorepo). Narrower than `readme-root` — it covers one project, its
build commands, and its license posture.

Bilingual pair required. Reference sibling projects by canonical
name when the architecture is related.
