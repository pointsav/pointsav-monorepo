# changelog

A `CHANGELOG.md` file in keep-a-changelog format. Newest version
on top; one line per PATCH; one section per MINOR; one chapter per
MAJOR per the workspace versioning rule.

Each line says WHAT changed and WHY in a short sentence.
