# app-infrastructure-leased

Cartucho de infraestructura para nodos arrendados (Laptop B).

Ejecuta en `os-infrastructure`. Desplegado como `fleet-infrastructure-leased` en el nivel
de presentación y `fleet-infrastructure-leased-1` en la Red Privada de PointSav.

Parte de la familia `app-infrastructure-*`.
